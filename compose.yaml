services:
  mysql:
    image: 'mysql:latest'
    environment:
      - MYSQL_ROOT_PASSWORD=secret
      - MYSQL_USER=hr
      - MYSQL_PASSWORD=secret
      - MYSQL_DATABASE=hr
    ports:
      - '3306'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
